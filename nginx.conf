user root;
worker_processes auto;

events {
}

http {
    server {
        listen       80;
        server_name  127.0.0.1;

        location / {
            proxy_pass http://front:3000/;
        }
        location /api/ {
            proxy_pass http://back:8000/;
        }

    }
}

